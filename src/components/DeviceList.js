import React from 'react';
import DeviceItem from './DeviceItem';

const DeviceList = () => {
    return (
        <div>
            <h1>DeviceItem</h1>
            {/* <DeviceItem /> */}
        </div>
    );
};

export default DeviceList;
